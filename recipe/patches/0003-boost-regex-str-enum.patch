diff --git a/src/SMESH/src/SMESHUtils/SMESH_MGLicenseKeyGen.cxx b/src/SMESH/src/SMESHUtils/SMESH_MGLicenseKeyGen.cxx
index b058e86..7661ed1 100644
--- a/src/SMESH/src/SMESHUtils/SMESH_MGLicenseKeyGen.cxx
+++ b/src/SMESH/src/SMESHUtils/SMESH_MGLicenseKeyGen.cxx
@@ -209,9 +209,9 @@ namespace
 
       libraryFile._isURL = false;
       if ( boost::regex_match( libraryFile._name, matchResult, urlRegex ))
-        libraryFile._isURL = ( !matchResult.str( SCHEME    ).empty() &&
-                              !matchResult.str( AUTHORITY ).empty() &&
-                              !matchResult.str( PATH      ).empty() );
+        libraryFile._isURL = ( !matchResult.str( int( SCHEME    )).empty() &&
+                              !matchResult.str( int( AUTHORITY )).empty() &&
+                              !matchResult.str( int( PATH      )).empty() );
     }
     if(libraryFile._isURL)
       return true;
@@ -223,9 +223,9 @@ namespace
 
       libraryFile._isURL = false;
       if ( boost::regex_match( libraryFile._name, matchResult, urlRegex ))
-        libraryFile._isURL = ( !matchResult.str( HOST ).empty() &&
-                              !matchResult.str( PORT ).empty() &&
-                              !matchResult.str( PATH ).empty() );
+        libraryFile._isURL = ( !matchResult.str( int( HOST )).empty() &&
+                              !matchResult.str( int( PORT )).empty() &&
+                              !matchResult.str( int( PATH )).empty() );
     }
     return libraryFile._isURL;
   }
