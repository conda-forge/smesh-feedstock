diff --git a/CMakeLists.txt b/CMakeLists.txt
index 21cb116..28c1616 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -71,6 +71,125 @@ endif()
 message(STATUS "VTK include directories: ${VTK_INCLUDE_DIRS}")
 include_directories(${VTK_INCLUDE_DIRS})
 
+# overwrite VTK_LIBRARIES
+set(VTK_LIBRARY_SUFFIX "-8.1.2")
+set(VTK_LIBRARIES
+    vtksys${VTK_LIBRARY_SUFFIX}
+    vtkCommonCore${VTK_LIBRARY_SUFFIX}
+    vtkCommonMath${VTK_LIBRARY_SUFFIX}
+    vtkCommonMisc${VTK_LIBRARY_SUFFIX}
+    vtkCommonSystem${VTK_LIBRARY_SUFFIX}
+    vtkCommonTransforms${VTK_LIBRARY_SUFFIX}
+    vtkCommonDataModel${VTK_LIBRARY_SUFFIX}
+    vtkCommonColor${VTK_LIBRARY_SUFFIX}
+    vtkCommonExecutionModel${VTK_LIBRARY_SUFFIX}
+    vtkCommonComputationalGeometry${VTK_LIBRARY_SUFFIX}
+    vtkFiltersCore${VTK_LIBRARY_SUFFIX}
+    vtkFiltersGeneral${VTK_LIBRARY_SUFFIX}
+    vtkImagingCore${VTK_LIBRARY_SUFFIX}
+    vtkImagingFourier${VTK_LIBRARY_SUFFIX}
+    vtkFiltersStatistics${VTK_LIBRARY_SUFFIX}
+    vtkFiltersExtraction${VTK_LIBRARY_SUFFIX}
+    vtkInfovisCore${VTK_LIBRARY_SUFFIX}
+    vtkFiltersGeometry${VTK_LIBRARY_SUFFIX}
+    vtkFiltersSources${VTK_LIBRARY_SUFFIX}
+    vtkRenderingCore${VTK_LIBRARY_SUFFIX}
+    vtkRenderingFreeType${VTK_LIBRARY_SUFFIX}
+    vtkRenderingContext2D${VTK_LIBRARY_SUFFIX}
+    vtkChartsCore${VTK_LIBRARY_SUFFIX}
+    vtkDICOMParser${VTK_LIBRARY_SUFFIX}
+    vtkdoubleconversion${VTK_LIBRARY_SUFFIX}
+    vtklzma${VTK_LIBRARY_SUFFIX}
+    vtkIOCore${VTK_LIBRARY_SUFFIX}
+    vtkIOLegacy${VTK_LIBRARY_SUFFIX}
+    vtkIOXMLParser${VTK_LIBRARY_SUFFIX}
+    vtkDomainsChemistry${VTK_LIBRARY_SUFFIX}
+    vtkglew${VTK_LIBRARY_SUFFIX}
+    vtkRenderingOpenGL2${VTK_LIBRARY_SUFFIX}
+    vtkDomainsChemistryOpenGL2${VTK_LIBRARY_SUFFIX}
+    vtkIOXML${VTK_LIBRARY_SUFFIX}
+    vtkParallelCore${VTK_LIBRARY_SUFFIX}
+    vtkFiltersAMR${VTK_LIBRARY_SUFFIX}
+    vtkFiltersFlowPaths${VTK_LIBRARY_SUFFIX}
+    vtkFiltersGeneric${VTK_LIBRARY_SUFFIX}
+    vtkImagingSources${VTK_LIBRARY_SUFFIX}
+    vtkFiltersHybrid${VTK_LIBRARY_SUFFIX}
+    vtkFiltersHyperTree${VTK_LIBRARY_SUFFIX}
+    vtkImagingGeneral${VTK_LIBRARY_SUFFIX}
+    vtkFiltersImaging${VTK_LIBRARY_SUFFIX}
+    vtkFiltersModeling${VTK_LIBRARY_SUFFIX}
+    vtkFiltersParallel${VTK_LIBRARY_SUFFIX}
+    vtkFiltersParallelImaging${VTK_LIBRARY_SUFFIX}
+    vtkFiltersPoints${VTK_LIBRARY_SUFFIX}
+    vtkFiltersProgrammable${VTK_LIBRARY_SUFFIX}
+    vtkPythonInterpreter${VTK_LIBRARY_SUFFIX}
+    vtkWrappingTools${VTK_LIBRARY_SUFFIX}
+    vtkWrappingPythonCore${VTK_LIBRARY_SUFFIX}
+    vtkFiltersPython${VTK_LIBRARY_SUFFIX}
+    vtkFiltersSMP${VTK_LIBRARY_SUFFIX}
+    vtkFiltersSelection${VTK_LIBRARY_SUFFIX}
+    vtkFiltersTexture${VTK_LIBRARY_SUFFIX}
+    vtkFiltersTopologyverdict${VTK_LIBRARY_SUFFIX}
+    vtkFiltersVerdict${VTK_LIBRARY_SUFFIX}
+    vtkmetaio${VTK_LIBRARY_SUFFIX}
+    vtkIOImage${VTK_LIBRARY_SUFFIX}
+    vtkImagingHybrid${VTK_LIBRARY_SUFFIX}
+    vtkInfovisLayout${VTK_LIBRARY_SUFFIX}
+    vtkInteractionStyle${VTK_LIBRARY_SUFFIX}
+    vtkImagingColor${VTK_LIBRARY_SUFFIX}
+    vtkRenderingAnnotation${VTK_LIBRARY_SUFFIX}
+    vtkRenderingVolume${VTK_LIBRARY_SUFFIX}
+    vtkInteractionWidgets${VTK_LIBRARY_SUFFIX}
+    vtkViewsCore${VTK_LIBRARY_SUFFIX}
+    vtklibproj${VTK_LIBRARY_SUFFIX}
+    vtkGeovisCore${VTK_LIBRARY_SUFFIX}
+    vtkIOAMR${VTK_LIBRARY_SUFFIX}
+    vtkIOAsynchronous${VTK_LIBRARY_SUFFIX}
+    vtkpugixml${VTK_LIBRARY_SUFFIX}
+    vtkIOCityGML${VTK_LIBRARY_SUFFIX}
+    vtkIOEnSight${VTK_LIBRARY_SUFFIX}
+    vtkexodusII${VTK_LIBRARY_SUFFIX}
+    vtkIOExodus${VTK_LIBRARY_SUFFIX}
+    vtkgl2ps${VTK_LIBRARY_SUFFIX}
+    vtkRenderingGL2PSOpenGL2${VTK_LIBRARY_SUFFIX}
+    vtkIOExport${VTK_LIBRARY_SUFFIX}
+    vtkIOExportOpenGL2${VTK_LIBRARY_SUFFIX}
+    vtklibharu${VTK_LIBRARY_SUFFIX}
+    vtkIOExportPDF${VTK_LIBRARY_SUFFIX}
+    vtkIOGeometry${VTK_LIBRARY_SUFFIX}
+    vtkIOImport${VTK_LIBRARY_SUFFIX}
+    vtkIOInfovis${VTK_LIBRARY_SUFFIX}
+    vtkIOLSDyna${VTK_LIBRARY_SUFFIX}
+    vtkIOMINC${VTK_LIBRARY_SUFFIX}
+    vtkogg${VTK_LIBRARY_SUFFIX}
+    vtktheora${VTK_LIBRARY_SUFFIX}
+    vtkIOMovie${VTK_LIBRARY_SUFFIX}
+    vtkIONetCDF${VTK_LIBRARY_SUFFIX}
+    vtkIOPLY${VTK_LIBRARY_SUFFIX}
+    vtkIOParallel${VTK_LIBRARY_SUFFIX}
+    vtkIOParallelXML${VTK_LIBRARY_SUFFIX}
+    vtksqlite${VTK_LIBRARY_SUFFIX}
+    vtkIOSQL${VTK_LIBRARY_SUFFIX}
+    vtkIOSegY${VTK_LIBRARY_SUFFIX}
+    vtkIOTecplotTable${VTK_LIBRARY_SUFFIX}
+    vtkIOVeraOut${VTK_LIBRARY_SUFFIX}
+    vtkIOVideo${VTK_LIBRARY_SUFFIX}
+    vtkImagingMath${VTK_LIBRARY_SUFFIX}
+    vtkImagingMorphological${VTK_LIBRARY_SUFFIX}
+    vtkImagingStatistics${VTK_LIBRARY_SUFFIX}
+    vtkImagingStencil${VTK_LIBRARY_SUFFIX}
+    vtkInteractionImage${VTK_LIBRARY_SUFFIX}
+    vtkPythonContext2D${VTK_LIBRARY_SUFFIX}
+    vtkRenderingContextOpenGL2${VTK_LIBRARY_SUFFIX}
+    vtkRenderingImage${VTK_LIBRARY_SUFFIX}
+    vtkRenderingLOD${VTK_LIBRARY_SUFFIX}
+    vtkRenderingLabel${VTK_LIBRARY_SUFFIX}
+    vtkRenderingMatplotlib${VTK_LIBRARY_SUFFIX}
+    vtkRenderingVolumeOpenGL2${VTK_LIBRARY_SUFFIX}
+    vtkViewsContext2D${VTK_LIBRARY_SUFFIX}
+    vtkViewsInfovis${VTK_LIBRARY_SUFFIX}
+)
+
 
 # --------------------------------------------------------------------------- #
 # BOOST
