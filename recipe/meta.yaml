{% set name = "smesh" %}
{% set version = "8.3.0.2" %}
{% set sha256 = "77f2b075b4698d4518913ade8bb9815f9f03ebec88ea55bdc27966cc29477ecf" %}
{% set commit = "3550378838cfc37aeb1803d9cd19c8a3c11db33a" %}

package:
    name: {{ name }}
    version: {{ version }}

source:
    fn: {{ name }}_{{ version }}.tar.gz
    # url: https://github.com/LaughlinResearch/SMESH/archive/v{{ version }}.tar.gz
    url: https://github.com/LaughlinResearch/SMESH/archive/{{ commit }}.tar.gz
    sha256: {{ sha256 }}

build:
    number: 2
    skip: True  # [py < 36]

requirements:
    build:
        - {{ compiler('cxx') }}
        - {{ cdt('mesa-libgl-devel') }}  # [linux]
        - ninja
    host:
        - cmake
        - vtk
        - boost-cpp
        - occt
        - netgen
        - pthread-stubs  # [unix]
        - pthreads-win32  # [win]
        - python
        - tbb-devel
        # - libmed
    run:
        - vtk
        - boost-cpp
        - occt
        - netgen
        - pthread-stubs  # [unix]
        - pthreads-win32  # [win]
        # - libmed

test:
    requires:
        - cmake
        - ninja
    files:
        - CMakeLists.txt
    commands:
        - cmake -G "Ninja" .

about:
    home: https://github.com/LaughlinResearch/SMESH
    license: LGPL2.1
    license_family: LGPL
    license_file:  LICENSE.txt
    summary: A complete MESH framework based on the OCCT library.
    description: Standalone version of the meshing library from the Salome Platform.

extra:
    recipe-maintainers:
        - looooo
