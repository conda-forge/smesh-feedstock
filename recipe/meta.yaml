{% set name = "smesh" %}
{% set version = "8.3.0.3.test" %}
{% set sha256 = "265b7ee9b59391d7c22dfa9e2affe3a691ed7ed75c9ce3a04d309b36b4657597" %}
{% set build_number = 14 %}
{% set build_number = build_number + 500 %}  # [FEATURE_DEBUG]

package:
    name: {{ name }}
    version: {{ version }}

source:
    fn: {{ name }}_{{ version }}.tar.gz
    url: https://github.com/looooo/SMESH/archive/netgen6.2.2004.tar.gz
    sha256: {{ sha256 }}
    # git_url: "https://github.com/looooo/SMESH"
    # git_tag: "netgen6.2.2004"

build:
    number: {{ build_number }}
    skip: True  # [py < 36]

requirements:
    build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - {{ cdt('mesa-libgl-devel') }}  # [linux]
        - {{ cdt('libxi-devel') }}       # [linux]
        - {{ cdt('libxext-devel') }}     # [linux]
        - ninja
        - cmake
    host:
        - vtk
        - boost-cpp
        - occt
        - netgen
        - python
        - tbb-devel
        - zlib
        - pthreads-win32  # [win]
        # - libmed
    run:
        - vtk
        - boost-cpp
        - occt
        - {{ pin_compatible('netgen', max_pin='x.x.x') }}
        - python
        - zlib
        - pthreads-win32  # [win]
        # - libmed

test:
    requires:
        - cmake
        - ninja
    files:
        - CMakeLists.txt
    commands:
        - cmake -G "Ninja" .

about:
    home: https://github.com/LaughlinResearch/SMESH
    license: LGPL-2.1
    license_family: LGPL
    license_file:  LICENSE.txt
    summary: A complete MESH framework based on the OCCT library.
    description: Standalone version of the meshing library from the Salome Platform.

extra:
    recipe-maintainers:
        - looooo
